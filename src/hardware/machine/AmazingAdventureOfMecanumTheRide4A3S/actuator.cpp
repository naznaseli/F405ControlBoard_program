#include "actuator.hpp"

namespace actuator
{
void setup(void)
{
    //dcMotor = new DcMotor[5];
    //dcMotor[0]->setup(can0, 0x100, 0);
    //dcMotor[1]->setup(can0, 0x100, 32);
    //dcMotor[2]->setup(can0, 0x101, 0);
    //dcMotor[3]->setup(can0, 0x100, 32);
    //dcMotor[4]->setup(can0, 0x102, 0);
    //airCylinder = new AirCylinder[8];

}

void update(void)
{
    //for(int i = 0; i < MOTOR_NUM; i++) motor[0]->update();
    //for(int i = 0; i < CYLINDER_NUM; i++) cylinder[i]->update();
    
}
 
}//namespace
